{% load pi_change_request_tags %}{{ user|full_name_with_username }} has responded "{{ response }}" to the PI change request for project
"{{ project_title }}" (Project ID: {{ project_id }}).

You can review PI change requests at the PI Change Request Center: {{ url }}
